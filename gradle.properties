org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties: https://fabricmc.net/develop
minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.16.14

# Mod Properties
mod_version=1.1.0
maven_group=dev.keyboard
archives_base_name=workstations

# Dependencies
fabric_version=0.92.12+1.20.1

# Compiled against, never required at runtime: only reached when MCA is actually installed.
# Held back because later builds need a newer Loom than this project uses. The handful of MCA
# methods called are unchanged since, so a newer MCA still runs against this.
mca_version=7.5.21+1.20.1+fabric
