org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Shared across every Minecraft version this branch builds.
mod_version=2.0.0
maven_group=dev.keyboard
archives_base_name=keyboard_workstations

# loom-back-compat picks remap loom below 26.1 and non-remap loom from 26.1 up.
loomx.loom_version=1.17.20
